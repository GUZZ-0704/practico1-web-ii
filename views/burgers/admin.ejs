<%- include('../../components/header.ejs') %>

<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="card mt-3">
                <div class="card-body">
                    <div class="card-title">
                        <h3>Lista de Hamburguesas</h3>
                    </div>
                    <div class="table-responsive">
                        <table class="table">
                            <thead class="table-dark">
                                <tr>
                                    <th>Id</th>
                                    <th>Nombre</th>
                                    <th>Precio</th>
                                    <th>Descripción</th>
                                    <th>Imagen</th>
                                    <th>Restaurante</th>
                                    <th></th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                <% burgers.forEach(burger => { %>
                                    <tr>
                                        <td>
                                            <%= burger.id %>
                                        </td>
                                        <td>
                                            <%= burger.name %>
                                        </td>
                                        <td>
                                            $<%= burger.price %>
                                        </td>
                                        <td>
                                            <%= burger.description %>
                                        </td>
                                        <td>
                                            <% if (burger.image) { %>
                                                <img src="/images/profile/<%= burger.restaurantId %>/<%= burger.id %>.jpg" alt="<%= burger.name %>" style="max-width: 100px;">
                                            <% } else { %>
                                                No image
                                            <% } %>
                                        </td>
                                        <td>
                                            <%= burger.restaurant?.name || 'No restaurant' %>
                                        </td>
                                        <td>
                                            <a class="btn btn-primary" href="/burgers/<%= burger.id %>/edit">Editar</a>
                                        </td>
                                        <td>
                                            <form method="post" action="/burgers/<%= burger.id %>/delete" onsubmit="return confirm('¿Está seguro que desea eliminar?')">
                                                <input class="btn btn-danger" type="submit" value="Eliminar">
                                            </form>
                                        </td>
                                    </tr>
                                <% }) %>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include('../../components/footer.ejs') %>
